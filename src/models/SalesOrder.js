const mongoose = require('mongoose');
const schema = new mongoose.Schema({ organizationId: { type: mongoose.Schema.Types.ObjectId, ref: 'Organization', required: true, index: true }, orderNumber: { type: String, required: true }, visitId: { type: mongoose.Schema.Types.ObjectId, ref: 'RetailVisit', required: true, index: true }, retailerId: { type: mongoose.Schema.Types.ObjectId, ref: 'Retailer', required: true, index: true }, employeeId: { type: String, required: true, index: true }, items: { type: [mongoose.Schema.Types.Mixed], default: [] }, summary: { type: mongoose.Schema.Types.Mixed, default: {} }, paymentTerms: { type: String, default: '' }, expectedDeliveryDate: { type: Date, default: null }, approvalRequired: { type: Boolean, default: false }, status: { type: String, enum: ['Draft', 'Submitted', 'Pending Approval', 'Approved', 'Confirmed', 'Processing', 'Packed', 'Dispatched', 'Partially Delivered', 'Delivered', 'Cancelled', 'Returned'], default: 'Submitted', index: true }, statusHistory: { type: [mongoose.Schema.Types.Mixed], default: [] } }, { timestamps: true });
schema.index({ organizationId: 1, orderNumber: 1 }, { unique: true });
module.exports = mongoose.model('SalesOrder', schema);
